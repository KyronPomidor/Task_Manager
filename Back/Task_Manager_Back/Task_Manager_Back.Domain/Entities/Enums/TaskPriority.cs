namespace Task_Manager_Back.Domain.Entities.Enums;

// LOL, I wanted it to be up to user to configure priorities, but then I realized that it would be too much work for a demo project
public enum TaskPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Urgent = 3
}
